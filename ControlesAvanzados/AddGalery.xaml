<Page
    x:Class="ControlesAvanzados.AddGalery"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ControlesAvanzados"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <StackPanel
            Padding="10"
            >
            <TextBlock Text="Agregar Imagen" 
                       Style="{StaticResource HeaderTextBlockStyle}"
                       Margin="0,0,0,10"
                       />    
            
            <TextBlock Text="Hoy" Style="{StaticResource CaptionTextBlockStyle}" 
                       Foreground="LightBlue"
                       Margin="0,0,0,10"
                       />

            <TextBox 
                x:Name="url"
                PlaceholderText="Url" Margin="0,0,0,10"/>
            
            <Button Content="Agregar" HorizontalAlignment="Right"/>
            
        </StackPanel>
        
    </Grid>
</Page>
